# pep2025-chinese-test-site

Knowledge test website for 2025 People’s Education Press Chinese Grade 8 syllabus.

## 运行方式

1. 在项目根目录执行 `node server.js`。
2. 浏览器访问 `http://localhost:4173` 即可体验测评页面。
3. 按 `Ctrl + C` 结束服务。

你也可以直接双击 `index.html` 用浏览器离线打开，但使用本地服务器可以确保所有资源路径表现一致。
